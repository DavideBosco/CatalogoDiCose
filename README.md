# CatalogoDiCose
A website presenting a catalogue of items taken from a JSON file. Made with SCSS, JavaScript and PHP.

in project directory run:
- `npm install`: to generate all the files
- `npm start`: to start a debug live server
- `npm run prepare`: to generate the production ready files